// const expect = require('expect');
// const generator = require('../../src/actions/index');

describe('actions to file', () => {
  describe('createAPIActionFile', () => {
    it('creates or appends a file');
    it('writes the generated file text to the file');
  });
});
